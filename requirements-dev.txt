# Dev/test/tooling dependencies. Runtime deps live in requirements.txt (that is
# all Streamlit Cloud installs). Install locally with:
#   pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0
ponytail>=0.9   # live log tailing for tools/follow_log.py
